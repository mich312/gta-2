# Anywhere City 768x768 from shared/src/world/city.data.ts
built-staircase     241,328,65      high   bridge deck edge at 274,361 climbs 8 treads averaging 5.6 tiles over 45 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     167,194,65      high   bridge deck edge at 200,226 climbs 9 treads averaging 5.0 tiles over 45 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     387,638,79      high   quay edge at 427,678 climbs 16 treads averaging 3.7 tiles over 59 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     614,356,52      high   quay edge at 640,382 climbs 5 treads averaging 6.4 tiles over 32 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     176,434,57      high   quay edge at 205,463 climbs 8 treads averaging 4.6 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     620,578,46      high   quay edge at 643,601 climbs 4 treads averaging 6.5 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     146,446,64      high   bridge deck edge at 178,478 climbs 12 treads averaging 3.7 tiles over 44 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     37,244,60       high   quay edge at 67,274 climbs 12 treads averaging 3.3 tiles over 40 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     253,284,42      high   bridge deck edge at 274,305 climbs 4 treads averaging 5.5 tiles over 22 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     315,724,44      high   quay edge at 337,756 climbs 5 treads averaging 4.8 tiles over 24 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     156,312,40      high   quay edge at 176,332 climbs 4 treads averaging 5.0 tiles over 20 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     368,651,39      high   quay edge at 388,671 climbs 4 treads averaging 4.8 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     62,442,39       high   yard edge at 82,462 climbs 4 treads averaging 4.8 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     149,356,46      high   quay edge at 172,379 climbs 8 treads averaging 3.3 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     445,720,37      high   quay edge at 464,739 climbs 4 treads averaging 4.3 tiles over 17 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     710,395,40      high   quay edge at 730,415 climbs 6 treads averaging 3.3 tiles over 20 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     188,417,38      high   quay edge at 207,436 climbs 6 treads averaging 3.0 tiles over 18 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     686,385,36      high   quay edge at 704,403 climbs 5 treads averaging 3.2 tiles over 16 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     268,656,71      med    quay edge at 304,692 climbs 18 treads averaging 2.8 tiles over 51 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     655,357,62      med    quay edge at 686,388 climbs 15 treads averaging 2.8 tiles over 42 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     186,502,57      med    quay edge at 214,531 climbs 13 treads averaging 2.8 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     36,370,57       med    quay edge at 65,399 climbs 14 treads averaging 2.6 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     520,304,46      med    quay edge at 543,327 climbs 10 treads averaging 2.6 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
built-staircase     641,435,39      med    quay edge at 660,455 climbs 7 treads averaging 2.7 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread, so every step here is drawn
# summary
# built-staircase         24  noisy
# TOTAL                   24
