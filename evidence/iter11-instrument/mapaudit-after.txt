# Anywhere City 768x768 from shared/src/world/city.data.ts
road-deadend        403,660,24      med    m=3        3-wide carriageway stops dead at 415,672 facing east into open ground — no landmark, quay or map edge to stop at
road-deadend        309,315,24      med    m=3        3-wide carriageway stops dead at 321,327 facing south into open ground — no landmark, quay or map edge to stop at
road-deadend        466,588,24      med    m=3        3-wide carriageway stops dead at 478,600 facing south into open ground — no landmark, quay or map edge to stop at
road-deadend        330,300,24      med    m=2        2-wide carriageway stops dead at 342,312 facing south into open ground — no landmark, quay or map edge to stop at
road-stops-short    252,373,24      med    m=9        3-wide carriageway at 264,385 stops 3 tile(s) short of the carriageway it runs at, heading east, with field+sidewalk across the mouth — the junction was never cut
road-stops-short    252,378,24      med    m=9        3-wide carriageway at 264,390 stops 3 tile(s) short of the carriageway it runs at, heading east, with field across the mouth — the junction was never cut
road-stops-short    252,395,24      med    m=9        3-wide carriageway at 264,407 stops 3 tile(s) short of the carriageway it runs at, heading east, with field across the mouth — the junction was never cut
road-stops-short    629,425,24      med    m=9        3-wide carriageway at 641,437 stops 3 tile(s) short of the carriageway it runs at, heading east, with field+sidewalk across the mouth — the junction was never cut
road-stops-short    253,540,24      med    m=9        3-wide carriageway at 265,552 stops 3 tile(s) short of the carriageway it runs at, heading east, with field across the mouth — the junction was never cut
road-stops-short    256,555,24      med    m=9        3-wide carriageway at 268,567 stops 3 tile(s) short of the carriageway it runs at, heading east, with field across the mouth — the junction was never cut
road-stops-short    270,395,24      med    m=9        3-wide carriageway at 282,407 stops 3 tile(s) short of the carriageway it runs at, heading west, with field across the mouth — the junction was never cut
road-stops-short    273,412,24      med    m=9        3-wide carriageway at 285,424 stops 3 tile(s) short of the carriageway it runs at, heading west, with field+sidewalk across the mouth — the junction was never cut
road-stops-short    277,463,24      med    m=9        3-wide carriageway at 289,475 stops 3 tile(s) short of the carriageway it runs at, heading west, with field across the mouth — the junction was never cut
road-stops-short    398,91,24       med    m=9        3-wide carriageway at 410,103 stops 3 tile(s) short of the carriageway it runs at, heading south, with sidewalk+field across the mouth — the junction was never cut
road-stops-short    415,108,24      med    m=9        3-wide carriageway at 427,120 stops 3 tile(s) short of the carriageway it runs at, heading north, with field across the mouth — the junction was never cut
road-stops-short    445,637,24      med    m=6        2-wide carriageway at 457,649 stops 3 tile(s) short of the carriageway it runs at, heading south, with field across the mouth — the junction was never cut
road-stops-short    491,628,24      med    m=8        2-wide carriageway at 503,640 stops 4 tile(s) short of the carriageway it runs at, heading south, with field across the mouth — the junction was never cut
built-staircase     241,328,65      high   m=37       bridge deck edge at 274,361 climbs 8 treads averaging 5.6 tiles over 45 tiles — a half-tile bevel only reaches a 1-tile tread. All 45 of its step faces (44 onto open water, 1 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     167,194,65      high   m=36       bridge deck edge at 200,226 climbs 9 treads averaging 5.0 tiles over 45 tiles — a half-tile bevel only reaches a 1-tile tread. All 45 of its step faces (45 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     387,638,79      high   m=43       quay edge at 427,678 climbs 16 treads averaging 3.7 tiles over 59 tiles — a half-tile bevel only reaches a 1-tile tread. All 59 of its step faces (59 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     614,356,52      high   m=27       quay edge at 640,382 climbs 5 treads averaging 6.4 tiles over 32 tiles — a half-tile bevel only reaches a 1-tile tread. All 32 of its step faces (0 onto open water, 32 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     176,434,57      high   m=29       quay edge at 205,463 climbs 8 treads averaging 4.6 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread. All 37 of its step faces (36 onto open water, 1 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     620,578,46      high   m=22       quay edge at 643,601 climbs 4 treads averaging 6.5 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread. All 26 of its step faces (26 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     146,446,64      high   m=32       bridge deck edge at 178,478 climbs 12 treads averaging 3.7 tiles over 44 tiles — a half-tile bevel only reaches a 1-tile tread. All 44 of its step faces (44 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     37,244,60       high   m=28       quay edge at 67,274 climbs 12 treads averaging 3.3 tiles over 40 tiles — a half-tile bevel only reaches a 1-tile tread. All 40 of its step faces (0 onto open water, 40 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     253,284,42      high   m=18       bridge deck edge at 274,305 climbs 4 treads averaging 5.5 tiles over 22 tiles — a half-tile bevel only reaches a 1-tile tread. All 22 of its step faces (22 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     315,724,44      high   m=19       quay edge at 337,756 climbs 5 treads averaging 4.8 tiles over 24 tiles — a half-tile bevel only reaches a 1-tile tread. All 24 of its step faces (24 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     156,312,40      high   m=16       quay edge at 176,332 climbs 4 treads averaging 5.0 tiles over 20 tiles — a half-tile bevel only reaches a 1-tile tread. All 20 of its step faces (0 onto open water, 20 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     368,651,39      high   m=15       quay edge at 388,671 climbs 4 treads averaging 4.8 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread. All 19 of its step faces (19 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     62,442,39       high   m=15       yard edge at 82,462 climbs 4 treads averaging 4.8 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread. 19 of its 19 step faces (0 onto open water, 19 onto dry ground) lie on no curve at all and are drawn square
built-staircase     149,356,46      high   m=18       quay edge at 172,379 climbs 8 treads averaging 3.3 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread. All 26 of its step faces (0 onto open water, 26 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     445,720,37      high   m=13       quay edge at 464,739 climbs 4 treads averaging 4.3 tiles over 17 tiles — a half-tile bevel only reaches a 1-tile tread. All 17 of its step faces (17 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     710,395,40      high   m=14       quay edge at 730,415 climbs 6 treads averaging 3.3 tiles over 20 tiles — a half-tile bevel only reaches a 1-tile tread. All 20 of its step faces (20 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     188,417,38      high   m=12       quay edge at 207,436 climbs 6 treads averaging 3.0 tiles over 18 tiles — a half-tile bevel only reaches a 1-tile tread. All 18 of its step faces (0 onto open water, 18 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     686,385,36      high   m=11       quay edge at 704,403 climbs 5 treads averaging 3.2 tiles over 16 tiles — a half-tile bevel only reaches a 1-tile tread. All 16 of its step faces (16 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     268,656,71      med    m=33       quay edge at 304,692 climbs 18 treads averaging 2.8 tiles over 51 tiles — a half-tile bevel only reaches a 1-tile tread. All 51 of its step faces (51 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     655,357,62      med    m=27       quay edge at 686,388 climbs 15 treads averaging 2.8 tiles over 42 tiles — a half-tile bevel only reaches a 1-tile tread. All 42 of its step faces (42 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     186,502,57      med    m=24       quay edge at 214,531 climbs 13 treads averaging 2.8 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread. All 37 of its step faces (0 onto open water, 37 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     36,370,57       med    m=23       quay edge at 65,399 climbs 14 treads averaging 2.6 tiles over 37 tiles — a half-tile bevel only reaches a 1-tile tread. All 37 of its step faces (37 onto open water, 0 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     520,304,46      med    m=16       quay edge at 543,327 climbs 10 treads averaging 2.6 tiles over 26 tiles — a half-tile bevel only reaches a 1-tile tread. All 26 of its step faces (0 onto open water, 26 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
built-staircase     641,435,39      med    m=12       quay edge at 660,455 climbs 7 treads averaging 2.7 tiles over 19 tiles — a half-tile bevel only reaches a 1-tile tread. All 19 of its step faces (0 onto open water, 19 onto dry ground) lie on a curve layer — coast, bank or deck — which all three painters cut against the chord, so NONE of this staircase is drawn
landuse-staircase   67,610,52       high   m=538      4024-tile wood whose boundary with open ground carries 538 tiles of flat tile edge, longest run 32, and 513 of its 792 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 65% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   479,43,34       high   m=227      827-tile wood whose boundary with open ground carries 227 tiles of flat tile edge, longest run 14, and 277 of its 298 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 93% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   437,593,37      high   m=173      430-tile wood whose boundary with open ground carries 173 tiles of flat tile edge, longest run 17, and 228 of its 228 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   424,22,45       high   m=140      466-tile wood whose boundary with open ground carries 140 tiles of flat tile edge, longest run 25, and 160 of its 160 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   487,647,32      high   m=140      662-tile wood whose boundary with open ground carries 140 tiles of flat tile edge, longest run 12, and 181 of its 182 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 99% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   458,602,41      high   m=123      767-tile wood whose boundary with open ground carries 123 tiles of flat tile edge, longest run 21, and 165 of its 165 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   501,586,46      high   m=118      234-tile wood whose boundary with open ground carries 118 tiles of flat tile edge, longest run 26, and 131 of its 131 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   410,55,45       high   m=114      567-tile wood whose boundary with open ground carries 114 tiles of flat tile edge, longest run 25, and 134 of its 150 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 89% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   294,670,36      high   m=88       192-tile wood whose boundary with open ground carries 88 tiles of flat tile edge, longest run 16, and 112 of its 115 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 97% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   498,599,42      high   m=77       155-tile wood whose boundary with open ground carries 77 tiles of flat tile edge, longest run 22, and 88 of its 88 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   519,70,29       high   m=73       246-tile wood whose boundary with open ground carries 73 tiles of flat tile edge, longest run 9, and 98 of its 98 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   528,590,33      high   m=68       146-tile wood whose boundary with open ground carries 68 tiles of flat tile edge, longest run 13, and 80 of its 80 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   410,629,44      high   m=68       181-tile wood whose boundary with open ground carries 68 tiles of flat tile edge, longest run 24, and 90 of its 90 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   359,651,27      high   m=64       240-tile wood whose boundary with open ground carries 64 tiles of flat tile edge, longest run 7, and 86 of its 94 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 91% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   453,725,32      med    m=63       386-tile wood whose boundary with open ground carries 63 tiles of flat tile edge, longest run 12, and 83 of its 86 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 97% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   556,621,41      med    m=54       111-tile wood whose boundary with open ground carries 54 tiles of flat tile edge, longest run 21, and 64 of its 64 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   440,630,28      med    m=50       139-tile wood whose boundary with open ground carries 50 tiles of flat tile edge, longest run 8, and 72 of its 72 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   493,93,27       med    m=47       289-tile wood whose boundary with open ground carries 47 tiles of flat tile edge, longest run 7, and 73 of its 73 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   483,648,31      med    m=47       114-tile wood whose boundary with open ground carries 47 tiles of flat tile edge, longest run 11, and 57 of its 57 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   582,604,33      med    m=46       114-tile wood whose boundary with open ground carries 46 tiles of flat tile edge, longest run 13, and 44 of its 54 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 81% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   443,664,31      med    m=46       340-tile wood whose boundary with open ground carries 46 tiles of flat tile edge, longest run 11, and 59 of its 69 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 86% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   580,597,36      med    m=44       187-tile wood whose boundary with open ground carries 44 tiles of flat tile edge, longest run 16, and 52 of its 70 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 74% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   473,67,27       med    m=42       233-tile wood whose boundary with open ground carries 42 tiles of flat tile edge, longest run 7, and 69 of its 69 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   298,664,24      med    m=41       83-tile wood whose boundary with open ground carries 41 tiles of flat tile edge, longest run 4, and 58 of its 58 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   115,581,27      med    m=38       106-tile wood whose boundary with open ground carries 38 tiles of flat tile edge, longest run 7, and 50 of its 50 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   525,43,32       med    m=37       91-tile wood whose boundary with open ground carries 37 tiles of flat tile edge, longest run 12, and 48 of its 48 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   397,735,28      med    m=33       400-tile wood whose boundary with open ground carries 33 tiles of flat tile edge, longest run 8, and 34 of its 43 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 79% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   600,578,30      med    m=29       65-tile wood whose boundary with open ground carries 29 tiles of flat tile edge, longest run 10, and 26 of its 44 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 59% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   514,61,32       med    m=28       61-tile wood whose boundary with open ground carries 28 tiles of flat tile edge, longest run 12, and 38 of its 38 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 100% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   317,720,26      med    m=27       266-tile wood whose boundary with open ground carries 27 tiles of flat tile edge, longest run 6, and 38 of its 40 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 95% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
landuse-staircase   247,641,25      med    m=25       287-tile wood whose boundary with open ground carries 25 tiles of flat tile edge, longest run 5, and 61 of its 63 boundary faces lie on NO smoothing layer at all — no coast, bank or deck chain describes a land-use edge and `bevel.ts` excludes T_TREES/T_FIELD in as many words ("woodland edges inland are left square too, for now"), so 97% of this outline is drawn as the tile squares it is stored as. The coastline is a tile mask too and steps at least as much, and none of ITS steps reaches the screen — `--selftest`'s land-use control measures both, on this bake, rather than quoting a figure that can go stale
course-unbuilt      350,588,180     high   m=169      Coast Road: 169 tiles of its 4-wide course carry no carriageway at all, from 360,685 to 520,681 (100% of it over open water) — the narrowest crossing here is longer than the plan's maxBridgeSpan of 72, so trimBridges took the deck back to water. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
course-unbuilt      518,282,101     high   m=81       Marsh Causeway: 81 tiles of its 4-wide course carry no carriageway at all, from 566,292 to 571,373 (100% of it over open water) — the course begins or ends out in the water, so a deck would have land on one side only. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
course-unbuilt      604,296,94      high   m=80       The Ring: 80 tiles of its 4-wide course carry no carriageway at all, from 649,306 to 652,380 (98% of it over open water) — the narrowest crossing here is longer than the plan's maxBridgeSpan of 72, so trimBridges took the deck back to water. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
course-unbuilt      530,614,95      high   m=79       Coast Road: 79 tiles of its 4-wide course carry no carriageway at all, from 542,675 to 612,648 (99% of it over open water) — the narrowest crossing here is longer than the plan's maxBridgeSpan of 72, so trimBridges took the deck back to water. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
course-unbuilt      597,299,92      high   m=77       The Ring: 77 tiles of its 4-wide course carry no carriageway at all, from 641,309 to 644,381 (98% of it over open water) — the narrowest crossing here is longer than the plan's maxBridgeSpan of 72, so trimBridges took the deck back to water. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
course-unbuilt      668,582,38      med    m=22       Coast Road: 22 tiles of its 4-wide course carry no carriageway at all, from 679,606 to 694,596 (94% of it over open water) — the course begins or ends out in the water, so a deck would have land on one side only. The tarmac at either end stops with a rounded cap over water, which reads to `road-deadend` as a quay; the plan is what says it is not one
street-serves-nothing  453,351,32      low    m=35       11.7-tile street from 469,361 to 469,373 meets no other centreline at either end, and its tarmac stops 3 and 1 tile(s) past them — a street whose both ends are terminal. Connectivity cannot see it: the carriageway is one component, so this IS reachable
street-serves-nothing  244,552,32      low    m=36       11.8-tile street from 254,568 to 266,568 meets no other centreline at either end, and its tarmac stops 2 and 3 tile(s) past them — a street whose both ends are terminal. Connectivity cannot see it: the carriageway is one component, so this IS reachable
street-serves-nothing  70,490,32       low    m=36       11.9-tile street from 80,505 to 91,508 meets no other centreline at either end, and its tarmac stops 3 and 1 tile(s) past them — a street whose both ends are terminal. Connectivity cannot see it: the carriageway is one component, so this IS reachable
street-serves-nothing  645,142,40      low    m=60       19.9-tile street from 669,153 to 660,171 meets no other centreline at either end, and its tarmac stops 2 and 1 tile(s) past them — a street whose both ends are terminal. Connectivity cannot see it: the carriageway is one component, so this IS reachable
street-serves-nothing  687,271,40      low    m=60       19.9-tile street from 711,282 to 704,301 meets no other centreline at either end, and its tarmac stops 3 and 1 tile(s) past them — a street whose both ends are terminal. Connectivity cannot see it: the carriageway is one component, so this IS reachable
lanes-serving-nothing  383,250,177     high   m=1198     393,312-549,365: 5749 tiles of land outside every district polygon carry 1198 carriageway tiles (20.8%) and 0 built tiles — lanes with nothing to drive to, because the fringe pass only places buildings within a district's own pitch of town (§14.6 D5) and this ground is in no district
lanes-serving-nothing  257,284,119     med    m=1140     267,312-365,375: 3237 tiles of land outside every district polygon carry 1140 carriageway tiles (35.2%) and 10 built tiles — lanes with nothing to drive to, because the fringe pass only places buildings within a district's own pitch of town (§14.6 D5) and this ground is in no district
# summary                   count    tiles      score      drawn
# road-deadend                  4       11       11.0       11.0
# road-stops-short             13      113      113.0      113.0
# road-speck                    0        0        0.0        0.0
# road-notch                    0        0        0.0        0.0
# road-width-jump               0        0        0.0        0.0  noisy x0.25
# junction-stub                 0        0        0.0        0.0
# walk-orphan                   0        0        0.0        0.0
# kerb-missing                  0        0        0.0        0.0
# crossing-missing              0        0        0.0        0.0
# shore-staircase               0        0        0.0        0.0
# built-staircase              24      540      135.0        3.8  noisy x0.25
# landuse-staircase            31     2708     2708.0     2432.0
# bare-corridor                 0        0        0.0        0.0
# patch-square                  0        0        0.0        0.0
# edge-notch                    0        0        0.0        0.0
# fabric-coarse                 0        0        0.0        0.0
# course-coverage-outlier       0        0        0.0        0.0
# course-unbuilt                6      508      508.0      508.0
# street-serves-nothing         5      227       56.8       56.8  noisy x0.25
# lanes-serving-nothing         2     2338     2338.0     2338.0
# country-outside-blocks        0        0        0.0        0.0
# carve-is-a-ruler              0        0        0.0        0.0
# TOTAL                        85     6445     5869.8     5462.5
# SCORE 5869.8, in weighted tiles of defect, against TOTAL 85 candidates.
# TOTAL is the count, and it was broken on purpose TWICE. Iteration 9 taught
# country-outside-blocks to ask the bake's own wildness field, removing a false positive.
# Iteration 11 ADDED two signatures for defects nothing here could see: course-unbuilt,
# the 508 tiles of authored road the bake has warned about on every run since the loop
# began, and landuse-staircase, the land-use fill no painter repaints. So a number from
# iterations 5-10 is on an older instrument and does not compare with one from this.
# Every bake the loop has is restated on THIS instrument in
# evidence/iter11-instrument/history.txt (iteration 9's two series are in
# evidence/iter9-instrument/history.txt); the deltas BETWEEN iterations are unchanged,
# because neither correction moved with the map.
# SCORE is what moves when a defect gets SMALLER without going away — which a count cannot see.
# SCORE is an area, so the signature covering the most ground dominates it; a small signature's
# progress shows in its own `tiles` column, which is the same measurement unweighted.
# DRAWN 5462.5 is the part of SCORE a renderer actually PUTS ON THE SCREEN.
# SCORE counts defects in the TILE PLANE — which is also what collision drives against — and
# a defect can be in the tile plane and painted over: a quay stepping every three tiles is
# repainted on a chord by the coast, bank or deck curve and cannot be seen. Those tiles stay
# in SCORE, because a renderer change can expose them again without one tile of ground moving,
# and are subtracted in DRAWN, because a reviewer sent to look at them will see nothing.
# The two differ only where a signature has MEASURED its own drawing, which today is
# `built-staircase` and `landuse-staircase`; every other signature is a defect in the
# ground, and ground is drawn. Those two are the same census pointed at different edges
# and they come out opposite: 97% of the built staircase is repainted and invisible,
# 90% of the land-use staircase is on no painter at all and is what the player sees.
# `landuse-staircase` is the first signature whose SCORE and DRAWN being EQUAL is a
# measurement rather than a default — and the reason a human found it and this did not.
